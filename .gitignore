node_modules/
dist/
build/
.expo/
*.log
.env
.env.local
.env.*.local
# Store credentials — never commit these
mobile/google-services.json
mobile/GoogleService-Info.plist
mobile/service-account.json
# Local Claude Code settings — may contain secrets in permission rules
.claude/settings.local.json
.DS_Store
*.orig.*
web-build/
android/
ios/
.gradle/
*.keystore
*.jks
*.p8
*.p12
*.cer
*.mobileprovision
*.orig.*
yarn-error.*

# Local Expo modules keep hand-written native code in ios/ android/ (the
# unanchored ignores above are for the root prebuild output, not these).
!mobile/modules/*/android/
!mobile/modules/*/ios/
